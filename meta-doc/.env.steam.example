# Steam 渠道 / 本地联调 Cloud Worker：将下列变量写入 meta-doc/.env（pnpm dev / electron-vite 会加载），
# 或复制本文件为 .env.steam 并在对应 mode 的构建中使用，或由 CI 注入。
# VITE_STEAM_DISTRIBUTION=true
# VITE_METADOC_CLOUD_API_URL=https://your-worker.workers.dev

# ---------- 仅 pnpm dev + 非 Steam 渠道包（VITE_STEAM_DISTRIBUTION 未为 true）----------
# 此时会走「DEV 假 Steam 登录」分支，否则会要求真实会话票据（需 Steam 渠道构建并从 Steam 启动）。
# Worker 侧须同时开启：wrangler.toml 中 ALLOW_DEV_AUTH = "true"，且已设置 secret：
#   cd cloudflare-worker && npx wrangler secret put DEV_AUTH_SECRET
# （自行设一串足够长的随机密码；没有「从 Valve 下载」的官方 key。）
# 客户端 .env 与 Worker 的 DEV_AUTH_SECRET 必须完全一致：
# VITE_METADOC_CLOUD_DEV_AUTH_SECRET=与_wrangler_secret_相同的那串
# VITE_METADOC_CLOUD_DEV_STEAM_ID=你的 SteamID64（十进制，如个人资料页 URL 里 76561198...）
# 联调 Microtransaction（充值页）：该 ID 必须与内嵌 Chromium 里登录的 Steam 账号一致，否则 Steam 侧易出现「交易授权错误」。
# Worker 的 STEAM_MICROTX_SANDBOX 须与 Steamworks 物品环境一致；STEAM_WEB_API_KEY 须为 Publisher Key。
# 生产 Worker 务必 ALLOW_DEV_AUTH=false，且勿把 DEV 密钥打进 Steam 商店包。
