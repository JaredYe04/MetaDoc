---
description: 容器组件使用 el-scrollbar 滚动条与自适应布局规范
globs: "**/*.vue"
alwaysApply: false
---

# 容器组件：滚动条与布局规范

## 1. 滚动条：统一使用 el-scrollbar

创建**任何可滚动的容器组件**时，必须用 Element Plus 的 `el-scrollbar` 包裹可滚动区域，而不是依赖浏览器默认滚动条或 `overflow: auto`。

- **原因**：统一视觉与交互，避免各平台滚动条样式不一致；便于控制 `max-height`、横向/纵向等。
- **注意**：需要滚动时再包一层；若容器本身不需要滚动（由内部子级滚动），则不必强行包。

```vue
<!-- ❌ 不推荐：裸 div + overflow -->
<div class="list-wrap" style="overflow: auto; max-height: 300px;">
  <div v-for="item in items" :key="item.id">{{ item.name }}</div>
</div>

<!-- ✅ 推荐：用 el-scrollbar 包裹可滚动区域 -->
<el-scrollbar class="list-scrollbar" max-height="300px">
  <div v-for="item in items" :key="item.id">{{ item.name }}</div>
</el-scrollbar>
```

样式上如需定制滚动条，使用 `:deep()` 针对 `.el-scrollbar__wrap` / `.el-scrollbar__view` 等。

## 2. 大容器：自适应窗口布局

若组件是**占据整屏或整块主区域的容器**，且内部有多个子容器，则：

- **不要**使用固定宽高（如 `width: 1200px`、`height: 800px`）。
- **应**使用弹性或百分比布局，随窗口变化自适应：
  - 使用 `flex` 布局（`flex: 1`、`min-height: 0` 等）填满父级；
  - 高度可用 `100%`、`100vh` 或父级 flex 分配；
  - 宽度同理用 `100%` 或 flex，避免写死 px。

这样在窗口缩放、布局变化时，大容器及其内部子区域会动态调整宽高，而不是溢出或留白。

```vue
<!-- ❌ 不推荐：大容器固定尺寸 -->
<div class="main-panel" style="width: 1200px; height: 800px;">
  <div class="sidebar">...</div>
  <div class="content">...</div>
</div>

<!-- ✅ 推荐：占满父级并自适应 -->
<div class="main-panel" style="display: flex; flex: 1; min-height: 0; width: 100%;">
  <aside class="sidebar" style="flex: 0 0 240px;">...</aside>
  <main class="content" style="flex: 1; min-width: 0;">
    <el-scrollbar style="height: 100%;">
      <!-- 内容 -->
    </el-scrollbar>
  </main>
</div>
```

**小结**：容器用 `el-scrollbar` 做滚动；整屏/大块容器用 flex + 百分比/100% 做自适应，不写死宽高。
