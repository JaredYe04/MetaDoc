---
description: 组件内所有用户可见字符串必须走 i18n，以中文为基准，默认提供中英文
globs: "**/*.vue"
alwaysApply: false
---

# 组件 i18n 规范

编写任何组件时，**所有面向用户的字符串**都必须通过 i18n 管理，不得在模板或脚本中硬编码中文/英文。

## 基准与语言

- **基准语言**：中文，对应 `src/renderer/src/locales/zh_cn.json`
- **默认需维护**：中文（zh_cn）+ 英文（en_us），即新增/修改文案时至少更新 `zh_cn.json` 与 `en_us.json`
- 其他语言（如 `ja_JP.json`、`ko_KR.json` 等）按需同步

## 文案存放位置

- 语言文件目录：`src/renderer/src/locales/`
- 中文基准文件：`zh_cn.json`
- 使用**点号路径**作为 key，按功能模块分块，例如：`leftMenu.save`、`contextMenu.copy`、`headMenu.home`

## 在组件中的用法

**模板中**：使用 `$t('key.path')`

```vue
<!-- ❌ 错误：硬编码中文 -->
<button>保存</button>
<span>关闭</span>

<!-- ✅ 正确：使用 i18n -->
<button>{{ $t('leftMenu.save') }}</button>
<span>{{ $t('mainTabs.close') }}</span>
```

**属性绑定**（如 title、placeholder、aria-label）：

```vue
<!-- ✅ 正确 -->
<el-tooltip :content="$t('leftMenu.fileTooltip')" />
<input :placeholder="$t('llmDialog.inputPlaceholder')" />
```

**脚本中**：通过 `useI18n()` 的 `t` 使用同一套 key

```ts
import { useI18n } from 'vue-i18n';

const { t } = useI18n();
message.success(t('leftMenu.exportAsTemplateSuccess'));
```

## 新增文案时的步骤

1. 在 **zh_cn.json** 中按模块添加 key，值为**中文**。
2. 在 **en_us.json** 中为相同 key 添加**英文**翻译。
3. 在组件中用 `$t('模块.key')` 或 `t('模块.key')` 引用，不要写死字符串。

## 示例：新增一个「提交」按钮

1. 在 `zh_cn.json` 的合适模块下（或新建模块）添加：`"submit": "提交"`
2. 在 `en_us.json` 同一路径添加：`"submit": "Submit"`
3. 模板中：`<button>{{ $t('form.submit') }}</button>`

## 例外

- 纯数字、符号、代码或配置用常量（如枚举值、API 字段名）可不走 i18n。
- 仅用于开发调试、不展示给最终用户的 log 文案可不走 i18n。
