# 示例文档（包含错误）

这是一个用于测试文本校对工具的示例Markdown文档，包含多种类型的错误。

## 第一章：介绍

这是第一章的内容。它包含了一些基本的文本，用于测试各种工具的功能。

### 1.1 子章节

这是子章节的内容。它展示了文档的层级结构。这里有一个拼写错误：teh（应该是the）。

## 第二章：主要内容

这是第二章的内容。它包含了更多的文本和结构。这里有一个语法错误：The cat is sleeping on the couch, it is very comfortable.（应该用分号或分成两句）

### 2.1 第一个子章节

这是第一个子章节的内容。这里有一个拼写错误：recieve（应该是receive）。

### 2.2 第二个子章节

这是第二个子章节的内容。这里有一个语法错误：I have went to the store yesterday.（应该是I went）

## 第三章：总结

这是第三章的内容，用于总结文档的主要内容。这里有一个拼写错误：seperate（应该是separate）。

### 3.1 结论

这是结论部分的内容。这里有一个语法错误：The books is on the table.（应该是books are）

## 代码示例

```javascript
function hello() {
  console.log('Hello, World!') // 这里有一个拼写错误：consol（应该是console）
}
```

## 列表示例

1. 第一项 - 这里有一个拼写错误：definately（应该是definitely）
2. 第二项 - 这里有一个语法错误：I can't hardly wait.（应该是I can hardly wait）
3. 第三项 - 这里有一个拼写错误：occured（应该是occurred）

## 表格示例

| 列1   | 列2   | 列3   |
| ----- | ----- | ----- |
| 数据1 | 数据2 | 数据3 |
| 数据4 | 数据5 | 数据6 |

这里有一个拼写错误：teh（应该是the）

## 引用示例

> 这是一个引用块的示例。这里有一个语法错误：Me and him went to the store.（应该是He and I went）

## 强调示例

这是**粗体文本**，这是*斜体文本*。这里有一个拼写错误：recieve（应该是receive）。

## 链接示例

这是一个[链接示例](https://example.com)。这里有一个拼写错误：exmaple（应该是example）。

## 图片示例

![示例图片](https://example.com/image.png)

这里有一个语法错误：The dog wagged it's tail.（应该是its tail）
